import 'package:flutter/material.dart';

class $PACKAGE_NAME_CAPITALLayout extends StatelessWidget{

  const $PACKAGE_NAME_CAPITALLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("$PACKAGE_NAME_CAPITAL")
    );
  }

}