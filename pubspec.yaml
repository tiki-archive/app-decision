name: $PACKAGE_NAME
description: $PACKAGE_NAME sdk for Tiki app.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  plugin:
    platforms:
      android:
        package: com.mytiki.$PACKAGE_NAME
        pluginClass: $PLUGIN_NAME
      ios:
        pluginClass: $PLUGIN_NAME
  assets:
    - res/
